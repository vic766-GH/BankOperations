[project]
name = "course-2"
version = "0.1.0"
description = ""
authors = [
    {name = "Victor Andreev",email = "766@mail.ru"}
]
requires-python = ">=3.14"
dependencies = [
    "black (>=26.5.1,<27.0.0)",
    "flake8 (>=7.3.0,<8.0.0)",
    "isort (>=8.0.1,<9.0.0)",
    "requests (>=2.34.2,<3.0.0)",
    "mypy (>=2.1.0,<3.0.0)",
    "openpyxl (>=3.1.5,<4.0.0)",
    "pytest-cov (>=7.1.0,<8.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest-cov (>=7.1.0,<8.0.0)"
]
